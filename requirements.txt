# ==============================================================================
# Core Runtime Dependencies for Deep Learning Misalignment Detection System
# AND Strad Carrier Monitoring Automation
# ==============================================================================
# 
# This file contains the minimum packages required to run the system in production.
# For development dependencies (testing, code quality), see requirements-dev.txt
#
# Installation: pip install -r requirements.txt
#
# Hardware Requirements:
# - Training: NVIDIA GPU with 8GB+ VRAM (CUDA 6.1+) or AMD GPU with ROCm
# - Inference: NVIDIA GPU with 4GB+ VRAM or NVIDIA Jetson (Xavier, Orin)
# - System RAM: 16GB for training, 8GB for inference
# - Platform: Windows 10/11 (recommended; screenshot capture is via headless
#   Chromium through Playwright, no OS-specific automation required)
# - GPU: CUDA 11.7+ (required for DL inference)
# ==============================================================================

# ------------------------------------------------------------------------------
# Deep Learning Framework
# ------------------------------------------------------------------------------
# PyTorch is the foundation for all neural network models in this system.
# Version 2.0+ is required for:
# - Native Automatic Mixed Precision (AMP) support for memory efficiency
# - Improved CUDA performance and memory management
# - Better support for gradient checkpointing
#
# Installation Notes:
# - For NVIDIA GPUs: This will install the CUDA-enabled version
# - For AMD GPUs: Follow PyTorch ROCm installation instructions at pytorch.org
# - For CPU-only (not recommended): Use torch==2.0.0+cpu from PyTorch website
torch>=2.0.0

# ------------------------------------------------------------------------------
# Computer Vision Utilities
# ------------------------------------------------------------------------------
# torchvision provides:
# - Image preprocessing and normalization (transforms)
# - Common dataset utilities
# - Pre-trained model components (we use some convolution utilities)
torchvision>=0.15.0

# ------------------------------------------------------------------------------
# Configuration Management
# ------------------------------------------------------------------------------
# PyYAML enables human-readable YAML configuration files for:
# - Model architecture selection (LiteFlowNet2 vs SpyNet)
# - Hyperparameters (learning rate, batch size, etc.)
# - Operational mode (neural_network, rule_based, hybrid)
# - Deployment settings (device, resolution, thresholds)
pyyaml>=6.0

# ------------------------------------------------------------------------------
# Training Visualization and Monitoring
# ------------------------------------------------------------------------------
# TensorBoard provides real-time training monitoring:
# - Loss curves (training and validation)
# - Learning rate schedules
# - GPU memory utilization
# - Sample prediction visualizations
# - Hyperparameter comparison across runs
tensorboard>=2.13.0

# ------------------------------------------------------------------------------
# Numerical Computing
# ------------------------------------------------------------------------------
# NumPy is used throughout for:
# - Efficient array operations and data manipulation
# - Image data preprocessing before PyTorch conversion
# - Ground truth label generation for augmented data
# - Evaluation metric calculations
numpy>=1.24.0

# ------------------------------------------------------------------------------
# Image Processing Libraries
# ------------------------------------------------------------------------------
# OpenCV (cv2) provides:
# - Additional image transformations for augmentation
# - Image I/O operations
# - Color space conversions
# - Geometric transformations (rotation, translation, affine)
opencv-python>=4.7.0

# Pillow provides:
# - Basic image file I/O (JPEG, PNG loading/saving)
# - Simple image transformations
# - Compatibility with PyTorch transforms
pillow>=9.5.0

# ------------------------------------------------------------------------------
# Data Validation and Configuration Schemas
# ------------------------------------------------------------------------------
# Pydantic enables:
# - Runtime type checking for configuration files
# - Automatic validation of YAML configuration parameters
# - Clear error messages when configuration is invalid
# - Structured data models for checkpoints and outputs
pydantic>=2.0.0

# ------------------------------------------------------------------------------
# Data Analysis and Visualization
# ------------------------------------------------------------------------------
# matplotlib for plotting and visualization:
# - Training curves and loss plots
# - Data distribution analysis
# - Network architecture diagrams
# - Optical flow visualization
matplotlib>=3.7.0

# seaborn for statistical visualizations:
# - Distribution plots
# - Correlation heatmaps
# - Dataset statistics
# - Beautiful default styling
seaborn>=0.12.0

# pandas for data manipulation and analysis:
# - Dataset statistics and summaries
# - Training metrics tracking
# - CSV file handling
# - DataFrame operations
pandas>=2.0.0

# ==============================================================================
# Strad Carrier Monitoring Automation Dependencies
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Connectivity
# ------------------------------------------------------------------------------
# pyodbc for SQL Server connectivity:
# - Production database interface
# - Parameterized queries for security
# - Connection pooling support
# Requires: ODBC Driver 17 for SQL Server (installed separately)
pyodbc>=5.0.0

# Alternative: pymssql (pure Python SQL Server driver)
# - No ODBC driver required
# - Useful for environments where ODBC installation is restricted
# Uncomment if using pymssql instead of pyodbc:
# pymssql>=2.2.0

# ------------------------------------------------------------------------------
# Windows COM Automation (Windows-only)
# ------------------------------------------------------------------------------
# pywin32 provides Windows-specific functionality (win32com.client, win32gui).
# No longer required by the active monitoring orchestrator (which now uses
# WebCapture/Playwright), but kept as a dependency because the standalone
# legacy modules src/strad_monitoring/excel_automation and
# src/strad_monitoring/vlc_capture still import it and have their own tests.
# Note: Only available on Windows platform
pywin32>=306; sys_platform == 'win32'

# ------------------------------------------------------------------------------
# Headless Browser Automation (Web-Based Screenshot Capture)
# ------------------------------------------------------------------------------
# Playwright drives a headless Chromium browser to navigate to each Axis
# camera's web viewer page, log in via the username/password form, and
# capture a full-window screenshot. Replaces Excel COM automation and direct
# RTSP frame-grabbing (RTSP access to these Axis encoders was confirmed
# unreliable - browser access fails outright, and VLC only shows a single
# corner of the feed for the same RTSP path).
#
# After `pip install playwright`, the browser binary must be installed once:
#   playwright install chromium
playwright>=1.40.0

# ------------------------------------------------------------------------------
# Task Scheduling
# ------------------------------------------------------------------------------
# APScheduler for hourly cycle scheduling:
# - Cron-like scheduling syntax
# - Blocking and background schedulers
# - Job persistence and recovery
apscheduler>=3.10.0

# ------------------------------------------------------------------------------
# Property-Based Testing (Development/Testing)
# ------------------------------------------------------------------------------
# Hypothesis for property-based tests:
# - Automated test case generation
# - Property verification across input space
# - Counterexample discovery
hypothesis>=6.90.0

# ------------------------------------------------------------------------------
# Testing Framework (Development/Testing)
# ------------------------------------------------------------------------------
# pytest for test execution:
# - Unit tests
# - Integration tests
# - Property-based tests
pytest>=7.4.0

# pytest-cov for code coverage:
# - Coverage reports
# - HTML coverage visualization
# - Coverage threshold enforcement
pytest-cov>=4.1.0

# ------------------------------------------------------------------------------
# Additional Notes for Strad Monitoring System
# ------------------------------------------------------------------------------
#
# SQL Server Connectivity:
# Requires ODBC Driver 17 for SQL Server to be installed separately.
# Download from: https://docs.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server
#
# Verify ODBC driver installation:
#   python -c "import pyodbc; print(pyodbc.drivers())"
#
# Web-Based Screenshot Capture (Playwright):
# Screenshots are captured by driving a headless Chromium browser to each
# camera's IP address (looked up from ip_addresses.json), logging into the
# Axis web viewer page, and taking a full-window screenshot. No Excel or
# VLC installation is required for this. After installing the playwright
# Python package, run `playwright install chromium` once to download the
# browser binary.
#
# Platform Requirements:
# - Windows 10 or Windows 11 (recommended)
# - Python 3.10 or 3.11 (compatibility with PyTorch 2.0+)
# - CUDA 11.7+ (for deep learning inference)
#
# Optional: SQLite Fallback
# The system includes SQLite fallback support for local testing when
# SQL Server is unavailable. No additional installation required - 
# SQLite is included with Python standard library.
#
# Memory Considerations:
# - PyTorch packages: ~3-5GB disk space
# - DL model checkpoints: 100MB-2GB per model
# - Snapshot storage: Plan for 100GB+ for 30-day retention
# - Database: Varies based on monitoring history retention

# ------------------------------------------------------------------------------
# Additional Notes
# ------------------------------------------------------------------------------
#
# CUDA Installation:
# PyTorch requires CUDA to be installed separately. Check compatibility:
# https://pytorch.org/get-started/locally/
#
# Typical CUDA versions:
# - PyTorch 2.0: CUDA 11.7 or 11.8
# - PyTorch 2.1+: CUDA 11.8 or 12.1
#
# Verify CUDA installation:
#   python -c "import torch; print(torch.cuda.is_available())"
#
# ROCm for AMD GPUs:
# If using AMD GPUs, follow ROCm installation instructions at:
# https://pytorch.org/get-started/locally/
# Then install PyTorch with ROCm support instead of CUDA version
#
# Memory Considerations:
# - These packages typically require 3-5GB disk space
# - PyTorch with CUDA support is the largest component (~2GB)
# - Model checkpoints will require additional 100MB-2GB per saved model
